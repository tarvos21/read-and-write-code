export LC_CTYPE="zh_CN.UTF-8"

# add this line to "~/.profile" to let fcitx work in emacs25
# 这一行是用 fcitx + sunpinyin 在 emacs25 中输入的
# 这样设置之后，可以输入中文

# this is also good to start emacs(for once):
LC_CTYPE=zh_CN.UTF-8 emacs25
